# CHANGELOG

## v2.4.5 (2026-09-14)
- **修复未出货未来批次误判为在途的缺陷**：
  - 生产总表中新增排单时，跟单员常在“实际出货时间”一并填入未来计划日期占位符（如 9/27）。原逻辑因 `ship_date is not None` 导致其在仓库无增量时被误判为“生产完毕在途”；
  - 修复为严格判定：只有在明确填报出货数量（`shipped_val > 0`）、或标记了“已完成/已清完”、或实际出货日期确实已在过去发生（`act_d <= today` 且不等于计划交期）时，才视为真正出厂并进入动态增量检测；
  - 未出厂批次（如 `WE13331348` 9/27批次、`WE043304` 9/21批次等）一律准确判定为 `🏭 生产中(预计MM-DD)`；
  - 增设 15 天在途时效过滤，避免数月前的远古出货被误判为当前在途。

## v2.4.4 (2026-09-14)
- **库存增减量动态监控引擎**：引入多期历史仓库快照的时间序列物理库存矩阵与本地持久化缓存，通过检测正向入库突增（$\Delta \text{Stock} > 0$）来判定翻单是否准确入库，彻底解决大负库存被入库冲抵后保有量依然极低引发的误判。
- **翻单批次状态机精准分流**：
  - 只有在仓库真正监测到匹配数量的正向增量后，才认定大货入库并从 Sheet 1 剔除；
  - 生产表填了出货/已完成但仓库未监测到增量的大货，系统一律打标为 `🚚 生产完毕在途` 并坚决保留在 Sheet 1（窗口期到货）中，自动校准有效到货日防止跌出窗口期；
- **客服专用表（Sheet 4）行动标签赋能**：状态列明确细分为 `🚚 生产完毕在途`、`✅ 已准确入库`、`🏭 生产制造中`，并升级右侧客服指引告示区。
- 引入仓库库存表动态列自适应匹配，支持根据表头定位核心字段，消除写死索引错误。
- 引入生意参谋多店铺自动识别机制，可基于 `config.json` 中的店铺关键词自动定位匹配数据子目录，支持在其子目录（如 7日商品数据/）下递归搜索报表。
- 引入近似颜色替代匹配功能，对于仓库中负库存且翻单表上无相同色号的SKC，能够自动匹配同款其他替代色号的翻单，在 Sheet1 中黄色背景高亮标注。
- 解决 `load_product_table` 中 `read_only` 模式误用 cell() 导致空值的缺陷，改用 `iter_rows` 方式读取。
- 解决跨年到货日期年份推算错误的问题（11-12月看见1-2月判定为明年，1-2月看见11-12月判定为去年）。
- 移除中文变量名及已停用的"商品状态"列相关的分类逻辑。
- 规范文件级导入、移除非必要局部导入，清理全部死代码与未使用变量。

## v4 (2026-06-02)
- 支持多 Sheet 动态表头识别，自适应列位置定位，解决生产进度表与翻单表因错列导致的数据提取错误。
- 自动兼容 SG品牌 与 NBA联名 等多子表结构。

## v3 (2026-05-18)
- 修复Sheet顺序：v2规则是 Sheet2=库存回补、Sheet3=无翻单需评分，但代码被改回旧顺序导致库存回补错在Sheet3
- 已将Sheet2和Sheet3对调，恢复v2正确结构

## v2 (2026-04-29)
- 修复 Sheet3 库存回补逻辑：款号必须在前一天表格里出现过才算，没出现过则跳过
- Sheet顺序调整为：Sheet1=窗口期到货、Sheet2=库存回补、Sheet3=无翻单需评分
- 新增品牌过滤：只处理 SPRAYGROUND 和 SPRAYGROUND X NBA 两个品牌

## v1 (2026-04-29)
- 新增 Sheet3 库存回补明细（对比当天与前一天仓库数据，可销<=0 → >0 的 SKC）
- 修复 config.json 路径（仓库/生意参谋/生产部表路径已更新）

