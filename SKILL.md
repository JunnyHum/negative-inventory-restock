# 负库存翻单到货分析技能

## 触发条件
用户更新数据源（生意参谋 / 仓库库存 / 生产部补单表）后，运行脚本分析。

## 输出归档
`~/Desktop/淘宝店铺/归档/窗口期到货_负库存/窗口期到货_负库存_YYYYMMDD.xlsx`

## 数据源
| 数据 | 路径 | 说明 |
|------|------|------|
| 仓库库存 | `~/Desktop/淘宝店铺/仓库商品库存表/系统库存导出_*.xlsx` | 自动匹配最新日期的库存表 |
| 仓库库存（备用） | `~/.openclaw/media/inbound/系统库存导出_*.xlsx` | 飞书通道接收到的最新备份文件 |
| SG品牌进度表 | `~/Desktop/淘宝店铺/生产部补单表/SG品牌进度表*.xlsx` | 包含 SG 品牌的各季订货与补货进度工作表 |
| NBA翻单数据 | `~/Desktop/淘宝店铺/生产部补单表/SG.NBA电商专供订单进度*.xlsx` | 电商联名专供进度表 |
| 生意参谋 | `~/Desktop/淘宝店铺/生意参谋数据/` 目录 | 依据 `config.json` 的店铺关键词自动匹配定位子目录数据 |
| 店铺商品表 | `~/Desktop/淘宝店铺/店铺商品表/SG网红店商品表.xlsx` | 包含各在售商品的上架时间等基础数据（用于回补分类判断） |

---

## 核心自适应匹配逻辑

### 1. 仓库表与生产进度表动态列匹配
脚本完全弃用了写死列索引的旧方式，基于表头关键字进行**自适应检测和匹配**（0-based列定位）：
- **款号**：寻找表头中包含 `款号` 或 `商品编码` 的列。
- **颜色**：寻找表头中包含 `颜色` 的列。
- **规格编码**：寻找表头中包含 `规格编码` 的列。
- **工厂**：寻找表头中包含 `工厂` 的列。
- **出货过滤**：寻找表头中包含 `总出货`、`已出货`、`出货数量` 的列，仅处理未出货记录（出货数 <= 0 或为空）。
- **到仓日期回退提取**：
  1. 优先提取：表头为 `实际到仓时间`、`实际出货时间` 的列值。
  2. 备选提取（若优先项为空）：表头为 `到仓时间`、`出货时间`、`货期` 的列值。
- **尺码匹配**：使用正则动态匹配 `XS`、`S`、`M`、`L`、`XL`、`2XL` 及 `均码` 的表头列。

### 2. 多 Sheet 自动遍历与无效表过滤
进度表（如 `SG品牌进度表.xlsx`）中常有多个品牌或联名 Sheet。脚本会自动遍历各 Sheet：
- 自动尝试从前 15 行中定位含有 `款号` 和 `颜色` 关键字的表头行。
- 若 Sheet 中不含上述关键字，则视为无效 Sheet 并自动跳过，不影响其他有效工作表的数据提取。

### 3. 生意参谋多店铺自动识别与递归搜索
- **路径自适应**：在 `config.json` 的 `shop` 中配置 `biz_keywords` 列表（例如 `["网红店", "sg网红"]`），脚本会在 `/Users/junny/Desktop/淘宝店铺/生意参谋数据/` 根目录下自动扫描并定位匹配该关键字的数据子目录。
- **文件递归搜索**：在匹配到的店铺目录下，脚本递归搜索匹配 `*生意参谋*.xls*` 的数据文件，能够自动深入子目录（例如 `7日商品数据/`）获取最新报表。

### 4. 近似颜色匹配（色号差异识别）
- 当某 SKC（10位，含2位色号）当前处于负库存/可销数低于预设值（<10）且生产部表无**该精确色号**翻单时：
- 脚本会扫描是否有**同款号（前8位）其他颜色**的翻单记录。
- 若存在，会在 **Sheet1「窗口期到货」** 中额外陈列出近似颜色行，以便人工评估替代可能。
- 近似匹配行在 Sheet1 中将以**浅黄色背景（`FFF2CC`）**显著标注，并在"生产状态"列中附加提示文字：`★色号差异：原[原色号]→替代[替代色号]，疑似替代`。

### 5. 到仓时间窗口期过滤
- 窗口期基于运行当天的日期设定为：**前10天 ~ 后15天**。
- 只有实际到仓时间（包含回退提取的时间）在窗口期内的翻单记录才会输出至 **Sheet1**。

---

## 输出报表结构说明

### Sheet1：「窗口期到货」
陈列负库存 SKC 及其在窗口期内有翻单到货安排的记录（包括近似颜色匹配行）：
- 正常匹配行背景为**浅绿**或**浅红**（负可销数警告）。
- 近似颜色替代匹配行背景为**浅黄**。
- 提供对应到货日期、生产状态、工厂。

### Sheet2：「库存回补」
对比当天与前一天库存数据，识别在昨日可销数 `<= 0` 且今日可销数 `> 0` 且在店铺正价表里出现过的 SKC。根据 **店铺商品表** 中的 **上架日期** 判定属性：
- **自动铺货**（上架日期在今天之后）：未来将要上架的新款，应准备铺货。
- **开启商品同步**（上架日期在今天之前或没有填写）：已上架的老款，应开启或维持库存同步。
- **新款待上架**（完全不在正价表中）：全新商品。

### Sheet3：「无翻单需评分」
陈列可销数 `< 10` 且最近 7 天内无任何翻单记录的 SKC。结合生意参谋的 7 天数据进行加权评分（访客 30% + 支付 50% + 加购 20%），并给出翻单建议（例如 `🔥 建议翻单`、`⚠️ 考虑翻单` 等）。

---

## 执行命令

```bash
cd ~/.openclaw/skills/negative-inventory-restock && python3 scripts/analyze.py
```

## 脚本列表
- [analyze.py](file:///Users/junny/.openclaw/skills/negative-inventory-restock/scripts/analyze.py)：负库存翻单到货分析核心控制脚本。
