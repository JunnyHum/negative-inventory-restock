# 版本管理规则

## 核心原则
修改 `scripts/` 下的任何 `.py` 前，**必须先创建版本备份**。
记忆记录 ≠ 完成，代码改了 ≠ 不用备份。

## 版本文件命名
```
analyze.py        ← 当前使用版本
analyze.v1.py     ← 备份版本（最旧）
analyze.v2.py
analyze.v3.py     ← 最新备份（最近一次修改前的版本）
```
版本号 = 该文件被修改的次数，从 1 开始递增。

## 保留规则
- 最多保留 **3 个历史版本**
- 超过 3 个时，删除最旧的 `.v{N}.py` 和对应 CHANGELOG 条目

## 版本创建流程
1. 读取当前 `analyze.py` 内容
2. 找到当前最高版本号 N（无则 N=0）
3. 创建 `analyze.v{N+1}.py` = 当前文件的完整副本
4. 更新 `CHANGELOG.md`（见下方格式）
5. 若版本数超过 3 个，删除最旧版本
6. 执行实际修改

## CHANGELOG.md 格式
```markdown
## v3 (2026-04-29)
- 新增 Sheet3 库存回补明细
- 对比两天仓库数据，前表可销<=0且后表>0的SKC

## v2 (2026-04-27)
- Sheet1/S2 格式调整
- 精简列数

## v1 (2026-04-23)
- 初始版本
```

## 回退操作
```bash
# 查看当前版本
cat VERSIONS.md

# 回退到 v2
cp scripts/analyze.v2.py scripts/analyze.py
```

## 触发条件
以下操作前必须创建版本：
- `edit` 任何 `.py` 文件
- 直接 `write` 覆盖 `.py` 文件
- `exec` 执行的命令会修改 `.py`（如 sed、python -c 写文件）

## ⚠️ 禁止事项
- 不得在创建版本前修改文件
- 不得跳过 CHANGELOG 记录
- 不得删除所有历史版本（至少保留 v1）
